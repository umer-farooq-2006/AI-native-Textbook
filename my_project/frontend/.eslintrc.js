module.exports = {
  root: true,
  extends: [
    'eslint:recommended',
    '@docusaurus',
  ],
  env: {
    browser: true,
    node: true,
  },
  rules: {
    // Add custom rules here if needed
  },
};